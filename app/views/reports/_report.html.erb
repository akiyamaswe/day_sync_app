<div id="<%= dom_id report %>">
  <p>
    <strong><%= Report.human_attribute_name(:user) %>:</strong>
    <%= link_to current_user_name(report.user), report.user %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:created_on) %>:</strong>
    <%= l report.created_on %>
  </p>

  <p>
    <strong><%= Report.human_attribute_name(:title) %>:</strong>
    <%= report.title %>
  </p>

  <p>
    <strong class="content-label"><%= Report.human_attribute_name(:content) %>:</strong>
    <%= raw Rinku.auto_link(h(format_content(report.content)), :all, nil, nil, Rinku::AUTOLINK_SHORT_DOMAINS) %>
  </p>
</div>
